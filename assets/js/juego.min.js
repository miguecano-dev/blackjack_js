const miModulo = (() => {
  'use strict'
  let e = [],
    t = ['C', 'D', 'H', 'S'],
    r = ['A', 'J', 'Q', 'K'],
    l = [],
    a = document.querySelector('#pedir_carta'),
    d = document.querySelector('#detener'),
    n = document.querySelector('#nuevo_juego'),
    s = document.querySelectorAll('.divCartas'),
    o = document.querySelectorAll('small'),
    i = (t = 2) => {
      ;(e = c()), (l = [])
      for (let r = 0; r < t; r++) l.push(0)
      o.forEach((e) => {
        e.innerText = 0
      }),
        s.forEach((e) => {
          e.innerHTML = ''
        }),
        (a.disabled = !1),
        (d.disabled = !1)
    },
    c = () => {
      e = []
      for (let l = 2; l <= 10; l++) for (let a of t) e.push(l + a)
      for (let d = 0; d < r.length; d++) for (let n of t) e.push(r[d] + n)
      return _.shuffle(e)
    },
    u = () => {
      if (0 === e.length) throw 'No hay cartas en el deck'
      return e.pop()
    },
    $ = (e) => {
      let t = e.substring(0, e.length - 1)
      return isNaN(t) ? ('A' === t ? 11 : 10) : 1 * t
    },
    g = (e, t) => ((l[t] = l[t] + $(e)), (o[0].innerText = l[t]), l[t]),
    h = (e, t) => {
      let r = document.createElement('img')
      ;(r.src = `assets/cartas/${e}.png`), r.classList.add('carta'), s[t].append(r)
    },
    f = (e) => {
      let t = 0
      do {
        let r = u()
        ;(t = g(r, l.length - 1)), h(r, l.length - 1)
      } while (t < e && e <= 21)
      p()
    },
    p = () => {
      let [e, t] = l
      setTimeout(() => {
        e === t
          ? alert('Nadie gana')
          : t > 21
          ? alert('computadora gana')
          : e > 21
          ? alert('jugador gana')
          : alert('computadora gana')
      }, 100)
    }
  return (
    a.addEventListener('click', () => {
      let e = u(),
        t = g(e, 0)
      h(e, 0),
        t > 21
          ? ((a.disabled = !0), (d.disabled = !0), f(t))
          : 21 === t && ((a.disabled = !0), (d.disabled = !0), f(t))
    }),
    d.addEventListener('click', () => {
      ;(a.disabled = !0), (d.disabled = !0), f(l[0])
    }),
    n.addEventListener('click', () => {
      i()
    }),
    { nuevoJuego: i }
  )
})()
